import { Request, Response, NextFunction, request } from 'express';


export class GetCountryController {
    GetCountry = async (
        request: Request,
        response: Response,
        next: NextFunction
    ) => {
        try{

        }catch {
            
        }
    }
}